# profileforge
Backend service that ingests inconsistent JSON profile records from multiple sources, validates and normalizes the data, detects and merges duplicate entities into canonical profiles, stores them in PostgreSQL, and exposes the results through a documented REST API. Built with automated tests, Docker, and clean Git practices.
